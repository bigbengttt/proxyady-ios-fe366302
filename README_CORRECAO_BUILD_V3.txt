CORRIGIDO V3

Correção aplicada:
- APIClient.swift: corrigido guard let payload que causava erro de compilação Swift / exit code 65 no GitHub Actions.
- Mantido Scheme compartilhado ProxyADY.
- Mantido workflow único .github/workflows/build-ipa.yml para gerar unsigned.ipa.

Envie este ZIP no bot novamente.
